{
  "name": "bootstrap-customizer",
  "version": "1.0.0",
  "description": "Create custom Bootstrap themes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "sass scss/style.scss css/style.css",
    "watch": "sass --watch scss/style.scss:css/style.css"
  },
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "sass": "^1.77.8"
  }
}
